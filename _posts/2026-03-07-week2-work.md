---
layout: post
title: "Week2 进展记录：RAG 摄取链路、分块与在线向量化"
date: 2026-03-07 10:00:00 +0800
categories: [til]
tags: [C++, PostgreSQL, pgvector, Redis, FastAPI, RAG, Docker]
---

# 背景

本周的工作重点是推进 RAG 项目的文档摄取链路。

当前项目架构如下：

- **C++ gateway**：负责对外 REST API、请求编排以及响应格式统一
- **Python AI service**：负责文档摄取、文本分块、向量化、检索与生成
- **PostgreSQL + pgvector**：负责存储元数据、分块内容与向量
- **Redis**：预留用于下一阶段的缓存与限流能力

本阶段采用分步实现策略：

1. 先稳定容器环境
2. 先打通不依赖 embedding 的摄取流程
3. 验证长文本场景下的分块行为
4. 接入在线 embedding
5. 确认向量已成功写入 pgvector

本周工作的核心原则如下：

> 先保证内部确定性链路可用，再接入外部模型 API。

这样做的目的在于控制问题定位范围，避免在同一阶段同时引入过多变量，降低调试复杂度。

# 环境准备

首先对容器环境进行了确认。目前已正常运行的服务包括：

- `postgres`
- `redis`
- `ai-service`
- `gateway`

同时配置了共享上传目录，用于保证：

- gateway 能够保存上传文件
- AI service 能够读取同一份文件并执行摄取

## 使用命令

```bash
mkdir -p uploads
docker compose down -v
docker compose up -d --build
docker compose ps
docker compose logs ai-service --tail=50
docker compose logs gateway --tail=50
```

# 数据库结构

在摄取阶段，先初始化了以下核心数据表：

- `docs`
- `tasks`
- `chunks`
- `rag_logs`

其中，一个关键设计点是：`chunks.embedding` 采用 `vector(1536)` 类型，因为当前选定的在线 embedding 模型返回 1536 维向量。

## 使用命令

```bash
docker compose exec postgres psql -U postgres -d ragkb
```

进入 `psql` 后执行：

```sql
\dt
\d docs
\d tasks
\d chunks
\dx
```

## 结构设计说明

- `docs`：业务实体，描述文档生命周期
- `tasks`：摄取任务状态
- `chunks`：RAG 检索使用的最小文本单元
- `rag_logs`：预留用于后续指标记录与查询日志

`docs` 与 `tasks` 的拆分是必要的，因为文档状态与任务状态属于不同维度的状态管理问题，语义上不应混合。

# 摄取流程骨架

在 Python AI service 中实现了 `/internal/ingest` 接口。

当前摄取流程如下：

1. 将任务状态更新为 running
2. 将文档状态更新为 processing
3. 读取源文件
4. 对文本执行分块
5. 将分块结果写入 PostgreSQL
6. 将任务状态更新为 success
7. 将文档状态更新为 ready

初始阶段未接入 embedding。这样安排的目的，是先验证文件读取、文本分块与数据库写入是否正确，再引入外部模型依赖。

# 长文本分块测试

短文本只能生成单个 chunk。虽然这足以说明摄取链路可执行，但不足以证明分块策略本身符合预期。

因此，为了验证分块逻辑，额外构造了一份较长的 Markdown 文档，并重新执行摄取测试。

## 分块参数

- `chunk_size = 700`
- `overlap = 100`

对应的滑动窗口步长为：

```text
step = 600
```

## 执行该测试的原因

长文本测试的价值主要体现在以下几个方面：

- 验证 chunk 数量是否大于 1
- 验证 chunk 索引是否连续
- 验证 overlap 是否实际生效
- 验证分块结果是否能够作为后续检索的有效输入单元

## 测试文件创建

```bash
cat > uploads/long_sample.md <<'EOF'
# RAG Platform Design

This document describes the design of a retrieval augmented generation platform built with a C plus plus gateway and a Python AI service. The gateway is responsible for external REST APIs, request validation, task orchestration, and unified response formatting. The Python service is responsible for document ingestion, text chunking, embedding generation, vector retrieval, and answer generation.

## Upload Flow

When a user uploads a document, the gateway stores the file in a shared upload directory and writes document metadata into the docs table. At the same time, the gateway creates a task record in the tasks table. The task starts in a queued state. The gateway then calls the internal ingestion endpoint exposed by the Python AI service. The Python service updates the task state to running and changes the document status from uploaded to processing.

## Chunking Strategy

The first implementation uses fixed size chunking with overlap. This approach is chosen because it is simple, predictable, and easy to debug. The chunk size is set to seven hundred characters and the overlap is set to one hundred characters. This means each new chunk starts six hundred characters after the previous chunk starts. The purpose of overlap is to reduce the risk that important information is split across chunk boundaries and becomes hard to retrieve later.

## Embedding and Storage

After chunking, each chunk will be sent to an online embedding model. The returned vector is validated against the configured embedding dimension. If the dimension does not match the database schema, the ingestion request fails and the error is written into the task record. Once validated, the text and embedding are written into the chunks table in PostgreSQL with pgvector enabled. This allows later similarity search using cosine distance.

## Query Flow

When a user submits a question, the gateway validates the request and checks whether the requested documents are ready. It then forwards the request to the Python AI service. The Python service embeds the question, performs vector retrieval on the chunks table, selects the top K chunks, and assembles a prompt. The prompt tells the language model to answer only based on the provided context. If the retrieved evidence is insufficient, the model should clearly say that the answer cannot be confirmed from the available documents.

## Citations

Each answer should include citations that point back to the supporting chunks. At minimum, each citation contains the document identifier, the chunk index, and a snippet of the chunk text. If page numbers are available, they should also be included. This makes the system more trustworthy because users can inspect the evidence used by the model.

## Reliability

The system is designed in stages. In the first stage, the team focuses on getting the full ingestion and query path working end to end. In the second stage, caching, rate limiting, structured logging, and evaluation are added. This staged design helps reduce risk because correctness is established before optimization. Failures in parsing, chunking, embedding, or database writes must all be surfaced clearly through task states and error messages.

## Operational Notes

The gateway and the AI service communicate over an internal network in Docker Compose. The services share an upload directory so that files written by the gateway can be read by the AI service. PostgreSQL stores metadata and vectors, while Redis is reserved for later use in caching and rate limiting. This architecture keeps the responsibilities of each component clear and makes it easier to replace the internal protocol in the future, for example by moving from internal HTTP calls to gRPC.
EOF
```

## 测试 SQL

```sql
INSERT INTO docs (title, source_path, owner, status)
VALUES ('long_sample.md', '/workspace/uploads/long_sample.md', 'wmr', 'uploaded')
RETURNING id;

INSERT INTO tasks (id, doc_id, status, progress, error)
VALUES ('22222222-2222-2222-2222-222222222222', 2, 'queued', 0, '');
```

## 测试请求

```bash
curl -X POST http://localhost:8000/internal/ingest \
  -H "Content-Type: application/json" \
  -d '{
    "taskId": "22222222-2222-2222-2222-222222222222",
    "docId": 2,
    "sourcePath": "/workspace/uploads/long_sample.md",
    "title": "long_sample.md"
  }'
```

## 校验 SQL

```sql
SELECT id, title, status
FROM docs
WHERE id = 2;

SELECT id, doc_id, status, progress, error
FROM tasks
WHERE id = '22222222-2222-2222-2222-222222222222';

SELECT id, doc_id, chunk_index, page, length(text) AS chunk_len
FROM chunks
WHERE doc_id = 2
ORDER BY chunk_index;
```

## 结果

长文本最终被成功拆分为 7 个 chunk。

这说明以下几点已经得到验证：

- chunk 索引连续
- 固定长度分块行为符合预期
- overlap 能够为后续检索保留跨边界上下文

# 在线 embedding 接入

在确认分块行为正确后，继续接入在线 embedding API。

本阶段采用 Qwen / DashScope 兼容接口，配置如下：

- endpoint：`https://dashscope.aliyuncs.com/compatible-mode/v1/embeddings`
- model：`text-embedding-v2`
- embedding dimension：`1536`

该配置与当前 pgvector 表结构保持一致。

## 为什么在完成分块验证之后再接入 embedding

这一阶段存在两类不同性质的问题来源：

1. 内部逻辑问题
2. 外部 API 问题

如果同时引入，排查路径会明显变复杂。因此实际执行时刻意分成两个阶段：

1. 先验证本地摄取链路
2. 再验证外部 embedding 调用链路

这种拆分有助于缩小问题范围，提高调试效率。

## 如何确认 embedding API 的确被调用

不能仅依据接口“看起来成功”来判断 embedding 已生效，因此在 embedding 调用前后增加了日志输出。

### 关键日志

```text
internal_ingest start task_id=66666666-6666-6666-6666-666666666666 doc_id=2 source_path=/workspace/uploads/long_sample.md title=log_test.md
read_text_file done source_path=/workspace/uploads/long_sample.md text_len=3748
chunk_text done text_len=3747 chunk_size=700 overlap=100 chunk_count=7
embed_texts start url=https://dashscope.aliyuncs.com/compatible-mode/v1/embeddings model=text-embedding-v2 text_count=7 expected_dim=1536
HTTP Request: POST https://dashscope.aliyuncs.com/compatible-mode/v1/embeddings "HTTP/1.1 200 OK"
embed_texts response status_code=200 elapsed_ms=597
embed_texts success vector_count=7 dim=1536
insert_chunks done doc_id=2 chunk_count=7
internal_ingest success task_id=66666666-6666-6666-6666-666666666666 doc_id=2 chunk_count=7 embedding_dim=1536
```

从这些日志可以确认：

- 请求已实际发出
- 请求已到达 DashScope
- 响应状态为 `200 OK`
- 返回结果中包含 7 条 embedding
- 向量维度与表结构一致
- 向量已进入 PostgreSQL 写入流程

# pgvector 写入验证

在日志验证之外，还对数据库状态进行了直接检查。

## 校验 SQL

```sql
SELECT chunk_index, vector_dims(embedding) AS dim
FROM chunks
WHERE doc_id = 2
ORDER BY chunk_index;

SELECT chunk_index, embedding IS NOT NULL AS has_embedding
FROM chunks
WHERE doc_id = 2
ORDER BY chunk_index;
```

## 结果

全部 7 个 chunk 均满足以下条件：

- `dim = 1536`
- `has_embedding = true`

这可以较强地说明：在线 embedding 请求已成功执行，且返回向量已被写入 pgvector。

# 一个更稳妥的 SQL 写法：使用 CTE 关联插入

测试过程中，曾手动为 `tasks` 写入 `doc_id`，结果由于使用了错误的 ID，触发了外键约束错误。

更稳妥的处理方式是通过 CTE 连接两次插入操作：

```sql
WITH new_doc AS (
  INSERT INTO docs (title, source_path, owner, status)
  VALUES ('cte_sample.md', '/workspace/uploads/long_sample.md', 'wmr', 'uploaded')
  RETURNING id
)
INSERT INTO tasks (id, doc_id, status, progress, error)
SELECT
  '44444444-4444-4444-4444-444444444444',
  id,
  'queued',
  0,
  ''
FROM new_doc;
```

其核心思路是：

> 如果 `tasks.doc_id` 依赖 `docs.id`，则应直接使用插入返回值传递该 ID，而不是手工猜测或填写。

这种方式更安全，也更符合原子性要求。

# 当前项目状态

截至目前，Week2 对应的 RAG 摄取阶段已完成以下内容：

- 环境准备
- PostgreSQL + pgvector 表结构初始化
- 摄取接口实现
- 长文本分块验证
- 在线 embedding 接入
- 向量写入验证
- 文档状态与任务状态流转打通

项目状态已不再停留于骨架搭建阶段，摄取数据链路已经具备可运行能力。

# 下一步计划

下一阶段的目标是实现最小可用查询链路，主要包括：

1. 对用户问题执行 embedding
2. 从 pgvector 检索 top K chunks
3. 按 `docScope` 进行过滤
4. 返回引用信息
5. 后续再接入生成模型

后续仍将延续相同原则：

> 先验证检索，再接入生成。

# 本周结论

本周的实现过程进一步确认了以下几点：

1. 文档状态与任务状态应当拆分管理
2. 本地摄取链路应先于外部模型 API 接入完成验证
3. 长文本是检验分块策略是否可靠的必要条件
4. 日志不应只覆盖服务启动过程，也应覆盖模型调用链路
5. SQL CTE 是处理关联插入的更安全方案
6. `vector_dims()` 与 `IS NOT NULL` 是验证 pgvector 写入结果的有效手段
